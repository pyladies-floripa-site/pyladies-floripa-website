import { getStackImagesByIds } from './photos'
import type { StackImage } from '../lib/imageStack'

export const aboutTimelinePhotos: StackImage[] = getStackImagesByIds([
  'grupo-sala-branca-21',
  'grupo-escritorio-9',
  'grupo-cafe-mesa',
  'grupo-escultura-externo',
  'grupo-bleachers-selfie',
])
